package it.unibs.fp.renderer;

public class RendererMain {

	public static void main(String[] args) {
		Vector r = new Vector(1,1,1);
		double radius = 1;
		
		r.normalise();
		r.scale(radius);
		/*
		y.setX(1);
		y.setY(r.getY() / r.getX());
		l = module_of_xy_projection(y.getX(), y.getY());
		L = module_of_xy_projection(r.getX(), r.getY());  
		y.setZ(r.getZ() * l / L);
		y.normalise();
		*/
		
	}
	
	/*
	private static double module_of_xy_projection(double x, double y) {
		return Math.sqrt(x*x + y*y);
	}
	*/
}
